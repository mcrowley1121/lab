#!/bin/bash
# Use default if variable is empty or unset
name="$1"
echo "Hello, ${name:-User}"
echo "$name"
# Hello, User

# Set the variable if unset or empty
name="$1"
echo "Hello, ${name:=User}"
echo "$name"
# Now contains "User"
